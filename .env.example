# KITOBMARKAZI — Environment Variables Example
PORT=3000
JWT_SECRET=your_random_secret_here

# The following keys can also be set in the Admin Dashboard Settings (SQLite)
# But setting them here can serve as defaults or overrides if implemented.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=
GEMINI_API_KEY=
OPENAI_API_KEY=
